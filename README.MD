# Installation
```sh
  cargo install cargo-crates
```

# Usage
```sh
  cargo crates # List all installed crates
  cargo crates -v # List all installed crates + their versions
  cargo crates -d # List all installed crates + their descriptions
  cargo crates -vd # List all installed crates + their descriptions and their versions
```
